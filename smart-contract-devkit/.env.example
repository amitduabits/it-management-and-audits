# =====================================================================
# Smart Contract DevKit - Environment Variables
# =====================================================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# The .gitignore file already excludes .env files.
# =====================================================================

# ---------------------------------------------------------------------
# Wallet Private Key
# ---------------------------------------------------------------------
# Your Ethereum wallet private key (without the 0x prefix).
# Used for deploying contracts to testnets/mainnet.
#
# How to get it:
#   1. Open MetaMask
#   2. Click the three dots next to your account
#   3. Select "Account Details" > "Show Private Key"
#   4. Enter your MetaMask password
#   5. Copy the key and paste it below
#
# SECURITY WARNING:
#   - Use a DEDICATED DEVELOPMENT WALLET, not your main wallet
#   - Never share this key with anyone
#   - Never commit this file to Git
#
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# ---------------------------------------------------------------------
# Alchemy API Key
# ---------------------------------------------------------------------
# Used for connecting to Ethereum networks via Alchemy's RPC endpoints.
#
# How to get it:
#   1. Create a free account at https://www.alchemy.com
#   2. Create a new app (select Ethereum > Sepolia)
#   3. Copy the API key from the app dashboard
#
ALCHEMY_API_KEY=your_alchemy_api_key_here

# ---------------------------------------------------------------------
# Etherscan API Key (Optional)
# ---------------------------------------------------------------------
# Used for verifying contracts on Etherscan after deployment.
#
# How to get it:
#   1. Create an account at https://etherscan.io
#   2. Go to API Keys in your profile
#   3. Create a new API key
#
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# ---------------------------------------------------------------------
# Gas Reporting (Optional)
# ---------------------------------------------------------------------
# Set to "true" to enable gas usage reporting during tests.
# Usage: REPORT_GAS=true npx hardhat test
#
REPORT_GAS=false
